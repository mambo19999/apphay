services:
  - type: web
    name: hayday-realtime
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -w 1 -b 0.0.0.0:$PORT wsgi:application
    envVars:
      - key: HD_API_KEY
        sync: false
